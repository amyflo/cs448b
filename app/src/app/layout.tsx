import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Exploring how love is expressed in r/LoveLetters",
  description:
    "r/LoveLetters is a subreddit where users share heartfelt stories of love, gratitude, and heartbreak. We explore how r/LoveLetters has grown and changed over time, examine sentiment patterns, and uncover themes thta emerge across love letters.",
  icons: {
    icon: "/favicon.ico", // Path to your favicon in the public folder
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
